# 🌊 DeepCurrents (深流)

**AI 驱动的全球情报聚合与宏观策略引擎**

> *新闻是水面上的浪花，趋势是深处的暗流。*

**[English](./README.en.md)** | 中文

DeepCurrents 是一个自动化的全球情报聚合与宏观研报生成系统。它从 35+ 个全球顶级资讯源（Reuters、Bloomberg、AP News、BBC、CNBC 等）中采集碎片化信息，经由威胁分类、事件聚类、趋势检测等多维分析管线，最终通过多智能体协作推理引擎合成为结构化的每日宏观策略研报。

---

## ✨ 核心特性

- **多维聚合**: 整合 35+ 全球顶级资讯源，覆盖地缘政治、宏观经济、能源大宗、央行政策、网络安全等维度，4 级可信度分级（T1-T4）。
- **多智能体协作**: 引入 **Macro Analyst** (地缘宏观专家) 与 **Sentiment Analyst** (市场情绪专家) 并行推理，由 **Market Strategist** (首席策略官) 汇总整合，提升研报深度。
- **实时行情注入**: 集成 `yfinance` 插件，自动获取黄金、原油、标普 500 等核心资产的实时价格，辅助 AI 进行逻辑与价格的“预期差”分析。
- **预测评分闭环**: 自动保存 AI 对资产趋势的研判，并在事后基于真实走势自动回测评分（0-100分），实现 AI 决策质量的持续量化。
- **异步高性能**: 全量 Python 3.10+ 重构，基于 `asyncio` 和 `aiohttp` 的非阻塞 I/O 设计，支持大规模并发采集。
- **非标源接入**: 支持通过 RSSHub 将 Telegram/站点动态转换为标准 RSS。
- **多通道分发**: 支持飞书富文本卡片和 Telegram Bot 双通道并行投递。

---

## 🚀 快速启动

### 前置要求

- **Python** >= 3.10
- 一个支持 OpenAI 兼容接口的 AI API Key

### 1. 克隆仓库

```bash
git clone https://github.com/your-username/DeepCurrents.git
cd DeepCurrents
```

### 2. 初始化环境

```bash
# 创建虚拟环境
python3 -m venv venv

# 安装依赖
./venv/bin/pip install -r requirements.txt
```

### 3. 配置环境变量

复制 `.env.example` 为 `.env` 并填写配置：

```bash
cp .env.example .env
```

### 4. 启动引擎

```bash
./venv/bin/python3 -m src.main
```

### 5. 运行测试

```bash
PYTHONPATH=. ./venv/bin/pytest
```

---

## 🏗️ 架构概览

```
                     ┌────────────────────┐
                     │    RSS Sources     │
                     │   (35+ 全球源)     │
                     └────────┬───────────┘
                              │ 每小时采集
                              ▼
┌──────────────────────────────────────────────────────┐
│              DeepCurrents Engine v2.2 (Python)       │
│                                                      │
│  ┌──────────┐  ┌───────────┐  ┌──────────────────┐  │
│  │Collector │─▶│ Circuit   │─▶│ SQLite + 模糊去重 │  │
│  │(aiohttp) │  │ Breaker   │  │ (trigram+Jaccard) │  │
│  └──────────┘  └───────────┘  └────────┬─────────┘  │
│                                        │             │
│  ┌──────────┐  ┌───────────┐           │             │
│  │Classifier│  │ Clustering│◀──────────┤             │
│  │ (威胁)   │  │ (聚类)    │           │             │
│  └──────────┘  └─────┬─────┘           │             │
│  ┌──────────┐        │          ┌──────┴──────┐      │
│  │ yfinance │────────┴─────────▶│ Multi-Agent │      │
│  │ (行情)   │                   │ Pipeline    │      │
│  └─────┬────┘                   └──────┬──────┘      │
│        │                               │             │
│        │          ┌──────────┐      结构化研报       │
│        └─────────▶│ Scorer   │◀────────┴──────────┐  │
│                   │ (自动评分)│                   │  │
│                   └──────────┘             ┌─────▼─────┐
│                                            │  Notifier │
│                                            └─────┬─────┘
│                                                  │
│                                        ┌─────────┴─────────┐
│                                        ▼                   ▼
│                                     Feishu              Telegram
└──────────────────────────────────────────────────────┘
```

---

## 📅 任务调度

| 任务 | 默认 Cron | 模块 | 说明 |
| :--- | :--- | :--- | :--- |
| **数据采集** | `0 * * * *` | `collector` | 每小时扫描所有 RSS 源，新增条目入库并去重 |
| **研报生成** | `0 8 * * *` | `engine` | 每天 08:00 合成全量情报，生成深度研报并推送 |
| **自动评分** | 每 4 小时 | `scorer` | 对历史 AI 预测进行真实行情回测评分 |
| **数据清理** | `0 3 * * *` | `db_service` | 每天 03:00 清理过期数据（默认保留 30 天） |

---

## 🔧 技术栈

| 组件 | 技术 | 说明 |
| :--- | :--- | :--- |
| **语言** | Python 3.10+ | 核心开发语言 |
| **运行时** | asyncio | 异步非阻塞运行时 |
| **AI SDK** | OpenAI Python SDK | 支持多提供商 + 结构化 JSON |
| **数据库** | aiosqlite | 异步 SQLite 交互 |
| **数据验证** | Pydantic v2 | 严格的运行时类型检查与校验 |
| **任务调度** | APScheduler | 强大的定时任务管理 |
| **日志** | Loguru | 现代化的结构化日志系统 |
| **NLP/分词** | Jieba + NLTK | 中英文多语言处理 |

---

## 📄 License

ISC

---

*Powered by DeepCurrents Intelligence Engine v2.2*
